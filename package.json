{
  "name": "simplelogs-express-example",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "SimpleLogs in an Express API \u2014 two middleware mounts",
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "dev": "node --watch --env-file-if-exists=.env src/server.js"
  },
  "dependencies": {
    "@simplelogs/express": "^2.0.0",
    "@simplelogs/node": "^2.0.0",
    "express": "^5.1.0"
  }
}
